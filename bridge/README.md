# Bridge MQTT -> WebSocket

## Run
```bash
npm install
npm run dev
```

## Env
- `MQTT_URL` default: `mqtt://captain.dev0.pandor.cloud:1884`
- `MQTT_TOPICS` default: `classroom/+/telemetry,flipper/+/+`
- `WS_PORT` default: `8080`
